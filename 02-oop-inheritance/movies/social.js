class Social {

    share(friendName){
        console.log('Share ' + this.title + ' with ' + friendName);
    }

    like(friendName){
        console.log('Like' + this.title + 'with' + friendName);
    }
}